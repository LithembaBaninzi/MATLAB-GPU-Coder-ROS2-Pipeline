# This folder contains MATLAB files used to generate the .so library

Inside you will find the following files:
- ***gpuGrayscale.m***, which contains the function of converting an RGB image to grayscale
- ***buildGpuGrayLib.m***, which is used to compile and generate the shared library 
